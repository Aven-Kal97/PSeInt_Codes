Algoritmo numeros_enteros
	//Los datos estructurado son aquellos que estab compuestos por datos simples 
	//x = NUMEROS ENTEROS
	definir A, i como entero;
	dimensionar A[5];
	//leer los numeros a guardar 
	para i  <- 0 hasta 4 hacer;
		leer A[i];
	FinPara
	//imprimir los numeros guardados
	para i <- 0 hasta 4 hacer;
		escribir A[i];
	FinPara
FinAlgoritmo
